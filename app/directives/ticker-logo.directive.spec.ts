import { TickerLogoDirective } from './ticker-logo.directive';

describe('TickerLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new TickerLogoDirective();
    expect(directive).toBeTruthy();
  });
});
